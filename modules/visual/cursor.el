;;; modules/visual/cursor.el -*- lexical-binding: t; -*-

;; I don't know if there's any way to do this in the theme itself, so for now
;; this one line of code gets its very own file
(setq evil-default-cursor (doom-color 'blue))
